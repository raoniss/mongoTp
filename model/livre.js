export default class Livre {
    constructor(titre, auteurs, année_de_publication, genres, quantité_disponible){
        this.titre = titre
        this.auteurs = auteurs
        this.année_de_publication = année_de_publication
        this.genres = genres
        this.quantité_disponible = quantité_disponible
    }
}

// module.exports = {Livre}